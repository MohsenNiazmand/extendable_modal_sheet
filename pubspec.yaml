name: extendable_modal_sheet
description: "Extendable Modal Sheet"
repository: https://github.com/MohsenNiazmand/extendable_modal_sheet
version: 1.0.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true